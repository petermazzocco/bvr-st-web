/**
 * Next.js cache tags for selective revalidation
 * Used with revalidateTag() to invalidate specific cached data when content changes
 */
export const TAGS = {
  /** Tag for general content-related cached data */
  content: "content",
};
